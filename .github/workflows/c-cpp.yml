name: C CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make

  deploy:

    runs-on: self-hosted

    steps:
    - name: build container
      run: docker build -t prodos .
    - name: run
      run: docker run -d -p 1977:1977/tcp prodos
